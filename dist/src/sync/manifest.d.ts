import type { SyncConfig, SyncManifest, SyncStatus } from "./types.js";
export declare function buildSyncManifest(config?: SyncConfig): SyncManifest;
export declare function getSyncStatus(config?: SyncConfig): SyncStatus;
